{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); echo \"$input\" | grep -q 'git commit' && echo 'CLAUDE.md CHECK REQUIRED: Before committing, review .claude/CLAUDE.md. Update it if any architectural decisions, documents, conventions, phase plan, or project status have changed in this session.' || true"
          }
        ]
      }
    ]
  }
}
