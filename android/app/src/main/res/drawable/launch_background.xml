<?xml version="1.0" encoding="utf-8"?>
<!-- Window background while the Flutter engine starts: the app's page colour
     (ScribeTokens.cream) so the launch does not flash white. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <color android:color="#FAF6EE" />
    </item>
</layer-list>
